#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Prints all single numbers of base 10 starting from 0
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	printf("%d", i);
	return (0);
}
